%h1 Jobs

%div
  -if @jobs.empty?
    No Jobs Queued
  -else
    %h2 In the queue:
  -@jobs.each do |job|
    %li
      =job.id
      %pre
        =truncate(job.last_error, :length=>400)

%div
  %h2 History:
  -if @job_logs.empty?
    No Jobs Logged
  -else 
    %table
      %thead
        %tr
          %th ID
          %th Name
          %th Status
          %th Updated
          %th Message
      %tbody
      -@job_logs.each do |job|
        %tr
          %td=link_to job.id, job
          %td=job.name
          %td=job.status
          %td=job.updated_at
          %td=job.message

