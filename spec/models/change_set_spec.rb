require 'spec_helper'

describe ChangeSet do
  it "should belong to a pool"
  it "should have previous changeset"
  it "should order by timestamp"
  it "should have a hash of persistent object_ids and their current version"
  
end
