require 'spec_helper'

describe Node do
  it "should store a hash of data"
  it "should create a new version when it's changed"
  it "should persist the persistent_id across identifiers"
  it "should create a new changeset when it's changed"
  it "should store it's parent"
  it "should show the difference between two versions"
end
